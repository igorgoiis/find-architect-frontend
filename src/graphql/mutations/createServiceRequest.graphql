mutation CreateServiceRequest($data: CreateServiceRequestInput!) {
  createServiceRequest(
    data: $data
  ){
    id,
    title,
    description,
    status,
    cRequestId,
    aRequestId
  }
}
